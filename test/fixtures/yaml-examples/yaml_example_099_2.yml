# --- [title] Example 8.4. / Chomping Final Line Break (2)

%YAML 1.2
---
!!map {
  ? !!str "strip"
  : !!str "text",
  ? !!str "clip"
  : !!str "text\n",
  ? !!str "keep"
  : !!str "text\n",
}

# --- [success] documents[0].content

{"strip":"text","clip":"text\n","keep":"text\n"}
# --- [title] YAML Example / 8.22. Block Collection Nodes (1)

sequence: !!seq
- entry
- !!seq
 - nested
mapping: !!map
 foo: bar

# --- [success] documents[0].content

{"sequence":["entry",["nested"]],"mapping":{"foo":"bar"}}
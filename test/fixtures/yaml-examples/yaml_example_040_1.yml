# --- [title] YAML Example / 5.12. Tabs and Spaces (1)

# Tabs and spaces
quoted: "Quoted 	"
block:	|
  void main() {
  	printf("Hello, world!\n");
  }

# --- [success] documents[0].content

{ "quoted": "Quoted \t", "block": "void main() {\n\tprintf(\"Hello, world!\\n\");\n}\n" }
# --- [title] Example 8.17. Explicit Block Mapping Entries

%YAML 1.2
---
!!map {
  ? !!str "explicit key"
  : !!str "",
  ? !!str "block key\n"
  : !!seq [
    !!str "one",
    !!str "two",
  ],
}

# --- [success] documents[0].content

{"explicit key":"","block key\n":["one","two"]}
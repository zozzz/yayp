# --- [title] Example 7.17. Flow Mapping Separate Values

{
unquoted·:·"separate",
http://foo.com,
omitted value:,
:·omitted key,
}

# --- [success] documents[0].content

{"unquoted·:·\"separate\"":null,"http://foo.com":null,"omitted value":null,":·omitted key":null}
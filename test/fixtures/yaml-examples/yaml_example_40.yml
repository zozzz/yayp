# --- [title] Example 5.7. Block Scalar Indicators

%YAML 1.2
---
!!map {
  ? !!str "literal"
  : !!str "some\ntext\n",
  ? !!str "folded"
  : !!str "some text\n",
}

# --- [success] documents[0].content

{"literal":"some\ntext\n","folded":"some text\n"}
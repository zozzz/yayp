# --- [title] YAML Example / 7.16. Flow Mapping Entries (2)

%YAML 1.2
---
!!map {
  ? !!str "explicit" : !!str "entry",
  ? !!str "implicit" : !!str "entry",
  ? !!null "" : !!null "",
}

# --- [success] documents[0].content

{"explicit":"entry","implicit":"entry","null":null}
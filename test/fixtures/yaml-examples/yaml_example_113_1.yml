# --- [title] YAML Example / 8.18. Implicit Block Mapping Entries (1)

plain key: in-line value
: # Both empty
"quoted key":
- entry

# --- [success] documents[0].content

{
  "plain key": "in-line value",
  "<null>": null,
  "quoted key": ["entry"]
}
# --- [title] YAML Example / 7.17. Flow Mapping Separate Values (1)

{
unquoted : "separate",
http://foo.com,
omitted value:,
: omitted key,
}

# --- [success] documents[0].content

{"unquoted":"separate","http://foo.com":null,"omitted value":null,"<null>":"omitted key"}
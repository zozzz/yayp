# --- [title] Custom Type / Namespace / Simple 2

%TAG !! tag:package.org,2016:
---
- !!world
  mapping: really
  x: y

# --- [success] documents[0].content

[
  {
    "$type": "!<tag:package.org,2016:world>",
    "$mapping": {
      "mapping": "really",
      "x": "y"
    }
  }
]